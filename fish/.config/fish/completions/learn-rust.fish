complete -c learn-rust -l init -d "Initialize new Rust workspace"
complete -c learn-rust -l name -r -d "Workspace name"
complete -c learn-rust -l lesson -r -d "Add new lesson (auto-numbered)"
complete -c learn-rust -l remove -r -d "Remove lesson by number"
complete -c learn-rust -l reset -d "Reset and remove all lessons"

